interface:
  display_name: "Solweaver"
  short_description: "Sol works solo or leads bounded agents"
  default_prompt: "Use $solweaver in auto mode with standard assurance unless risk or an explicit request requires final-strict; select solo, solo-reviewed, or team execution only when requested or materially useful."
