# Merge these values into ~/.codex/config.toml.
model = "gpt-5.6-sol"
model_reasoning_effort = "max"

[agents]
enabled = true
# This cap counts spawned-agent threads and excludes the primary Sol thread.
max_concurrent_threads_per_session = 2
